import { IfUserDirectiveDirective } from './if-user-directive.directive';

describe('IfUserDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IfUserDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
